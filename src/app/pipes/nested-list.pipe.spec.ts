import { NestedListPipe } from './nested-list.pipe';

describe('NestedListPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedListPipe();
    expect(pipe).toBeTruthy();
  });
});
