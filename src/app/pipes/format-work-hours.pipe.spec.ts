import { FormatWorkHoursPipe } from './format-work-hours.pipe';

describe('FormatWorkHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWorkHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
