import { ToLocalDEPipe } from './to-local-de.pipe';

describe('ToLocalDEPipe', () => {
  it('create an instance', () => {
    const pipe = new ToLocalDEPipe();
    expect(pipe).toBeTruthy();
  });
});
